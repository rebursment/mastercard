<form method="post" action="{{ path('app_identitee_delete', {'id': identitee.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ identitee.id) }}">
    <button class="btn">Delete</button>
</form>
